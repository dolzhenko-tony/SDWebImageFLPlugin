// swift-tools-version:5.1

import PackageDescription

  let package = Package(
     name: "SDWebImageFLPlugin",
     products: [
         .library(
             name: "SDWebImageFLPlugin",
             targets: ["SDWebImageFLPlugin"]),
     ],
     dependencies: [
     ],
     targets: [
         .target(
             name: "SDWebImageFLPlugin",
             dependencies: []),
     ]
 )
